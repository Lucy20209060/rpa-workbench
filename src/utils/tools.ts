export const mergeParams = (obj:object):string => {
	// const dataLen = Object.keys(obj).length;
	// if(dataLen === 0){
	// 	return ''
	// }else{
	// 	let str = '';
	// 	for(let item in obj){
	// 		str = str + `${item}=${obj[item]}&`
	// 	}
	// 	console.log(str)
		return ''
	// }
	
}